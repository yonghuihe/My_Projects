<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com._520it.es.mapper.UserInfoMapper" >
  <resultMap id="BaseResultMap" type="com._520it.es.domain.UserInfo" >
    <id column="id" property="id"  />
	<result column="sex" property="sex"  />
	<result column="nickname" property="nickname"  />
	<result column="privilege" property="privilege"  />
	<result column="unionid" property="unionid"  />
	<result column="province" property="province"  />
	<result column="openid" property="openid"  />
	<result column="headimgurl" property="headimgurl"  />
	<result column="country" property="country"  />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="long" >
    delete from userInfo where id = #{id}
  </delete>
  <insert id="insert" parameterType="com._520it.es.domain.UserInfo" useGeneratedKeys="true" keyProperty="id" >
    insert into userInfo 
    (sex,nickname,privilege,unionid,province,openid,headimgurl,country)
    values 
    (#{sex},#{nickname},#{privilege},#{unionid},#{province},#{openid},#{headimgurl},#{country})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com._520it.es.domain.UserInfo" >
    update userInfo
    set
	    sex=#{sex},
	    nickname=#{nickname},
	    privilege=#{privilege},
	    unionid=#{unionid},
	    province=#{province},
	    openid=#{openid},
	    headimgurl=#{headimgurl},
	    country=#{country}
    where id = #{id}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="long" >
    select * from userInfo where id = #{id}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    select * from userInfo
  </select>
  <sql id="conditionSql">
  	<where>
  	</where>
  </sql>
  <select id="queryByCondictionCount" resultType="long">
  	select count(*) from userInfo
  	<include refid="conditionSql"/>
  </select>
  <select id="queryByConditionResult" resultMap="BaseResultMap">
  	select * from userInfo
  	<include refid="conditionSql"/>
  	limit #{start},#{rows}
  </select>
</mapper>