package ${basePackage}.dao.impl;

import ${basePackage}.dao.I${className}Dao;
import ${basePackage}.domain.${className};

public class ${className}DaoImpl extends GenericDaoImpl<${className}> implements
		I${className}Dao {
}
